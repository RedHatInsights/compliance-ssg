# Contributing guide

In order to add content to this repository, the following workflow is expected to be followed:

- Create a new feature branch for the new release
- Add the new content to the `content` directory. For getting the new playbooks:
  - Run the `generate_content` script, which should get the new version of playbooks according to the current config (either from upstream or downstream).
- Submit a PR with the new content
